<Page
    x:Name="pageRoot"
    x:Class="ClientApp.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClientApp"
    xmlns:common="using:ClientApp.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Button Content="A" HorizontalAlignment="Left" Margin="95,177,0,0" VerticalAlignment="Top" Height="180" Width="210" FontSize="72" FontWeight="Bold" BorderThickness="10" Grid.Row="1" Background="#FF3C1B76" BorderBrush="#FF204255"/>
        <Button Content="B" HorizontalAlignment="Left" Margin="418,177,0,0" VerticalAlignment="Top" Height="180" Width="210" FontSize="72" FontWeight="Bold" BorderThickness="10" Grid.Row="1" Background="#FF3C1B76" BorderBrush="#FF204255"/>
        <Button Content="C" HorizontalAlignment="Left" Margin="730,177,0,0" VerticalAlignment="Top" Height="180" Width="210" FontSize="72" FontWeight="Bold" BorderThickness="10" Grid.Row="1" Background="#FF3C1B76" BorderBrush="#FF204255"/>
        <Button Content="D" HorizontalAlignment="Left" Margin="1054,177,0,0" VerticalAlignment="Top" Height="180" Width="210" FontSize="72" FontWeight="Bold" BorderThickness="10" Grid.Row="1" Background="#FF3C1B76" BorderBrush="#FF204255"/>
        <TextBlock HorizontalAlignment="Left" Margin="1168,548,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Timer" VerticalAlignment="Top" Height="70" Width="188" FontSize="48"/>
    </Grid>
</Page>

